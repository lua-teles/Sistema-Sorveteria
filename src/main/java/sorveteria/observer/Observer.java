package sorveteria.observer;
import sorveteria.model.Pedido;

public interface Observer { //so implementar ai raissa as coisas !! criei aqui a interface e as classes pra te ajudar pois sou humilde <3
    public void update (Pedido pedido);
}
